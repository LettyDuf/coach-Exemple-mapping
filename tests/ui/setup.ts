/**
 * Setup commun aux tests UI (jsdom).
 *
 * Étend l'API d'assertions Vitest avec celles de @testing-library/jest-dom
 * (toBeInTheDocument, toHaveAccessibleName, etc.).
 */
import "@testing-library/jest-dom/vitest";
